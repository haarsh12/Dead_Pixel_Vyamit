╔════════════════════════════════════════════════════════════╗
║          MYKIRANA BACKEND - QUICK START GUIDE             ║
╚════════════════════════════════════════════════════════════╝

📦 SETUP (5 minutes)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Install Dependencies:
   cd backend_app
   python -m venv venv
   venv\Scripts\activate              # Windows
   pip install -r requirements.txt

2. Configure Environment:
   copy .env.example .env             # Windows
   Edit .env:
   - DATABASE_URL=your_supabase_url
   - SECRET_KEY=generate_with_python
   - GEMINI_API_KEY=your_gemini_key

3. Setup Database (Supabase):
   CREATE EXTENSION IF NOT EXISTS vector;

4. Verify Setup:
   python verify_setup.py

5. Start Server:
   python main.py
   # Or: .\start.ps1 (Windows) / ./start.sh (Linux)

🌐 ACCESS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

API Docs:  http://localhost:8000/docs
Health:    http://localhost:8000/health
Info:      http://localhost:8000/info

🔑 GENERATE SECRET_KEY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

python -c "import secrets; print(secrets.token_urlsafe(32))"

🗄️ API ENDPOINTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Authentication:
  POST /auth/request-otp
  POST /auth/verify-otp
  GET  /auth/profile
  PUT  /auth/profile

Inventory:
  POST   /items/
  GET    /items/
  PUT    /items/{id}
  DELETE /items/{id}
  POST   /items/bulk-embed

Analytics:
  POST /analytics/bills
  GET  /analytics/bills
  GET  /analytics/dashboard
  GET  /analytics/overview

RAG Voice AI:
  POST /rag/query
  POST /rag/query-simple
  GET  /rag/status

SMS:
  POST /sms/send-bill
  POST /sms/send-custom

🔐 TESTING AUTHENTICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Request OTP:
   POST /auth/request-otp
   {
     "phone_number": "9876543210",
     "is_login": false
   }

2. Verify OTP (Demo Mode):
   POST /auth/verify-otp
   {
     "phone_number": "9876543210",
     "otp_code": "112233",
     "shop_name": "Test Shop",
     "shop_category": "Kirana"
   }

3. Get Token → Use in Authorization header

🚀 DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Render/Railway:
  Start Command:
    uvicorn main:app --host 0.0.0.0 --port $PORT
  
  Environment Variables:
    DATABASE_URL, SECRET_KEY, GEMINI_API_KEY,
    MISTRAL_API_KEY, FAST2SMS_API_KEY

🛠️ TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ Database Error:
   → Check DATABASE_URL
   → Verify pgvector enabled
   → Check Supabase connection

❌ Embedding Error:
   → Verify GEMINI_API_KEY
   → Check API quota

❌ Import Errors:
   → Activate venv
   → pip install -r requirements.txt

✅ OTP Demo Mode:
   → Set OTP_DEMO_MODE=1 in .env
   → Use OTP: 112233

📋 WHAT'S INCLUDED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Complete REST API (5 modules)
✅ JWT Authentication
✅ OTP Verification
✅ Inventory Management
✅ Analytics Dashboard
✅ RAG Voice AI (Gemini embeddings)
✅ SMS Integration
✅ Vector Search (pgvector)
✅ Clean Architecture
✅ Production Ready

📚 DOCUMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

BACKEND_COMPLETE.txt  - Full implementation report
.env.example          - Configuration template
verify_setup.py       - Setup verification

🎯 STATUS: ✅ COMPLETE & READY TO USE

Need help? Check the logs or run verify_setup.py
